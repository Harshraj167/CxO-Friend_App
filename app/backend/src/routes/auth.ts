import express from 'express'
const router = express.Router()

// Placeholder route file: auth.ts
router.get('/', (req, res) => res.json({ message: 'auth route placeholder' }))

export default router
