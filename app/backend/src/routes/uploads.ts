import express from 'express'
const router = express.Router()

// Placeholder route file: uploads.ts
router.get('/', (req, res) => res.json({ message: 'uploads route placeholder' }))

export default router
