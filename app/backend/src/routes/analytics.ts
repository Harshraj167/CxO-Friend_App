import express from 'express'
const router = express.Router()

// Placeholder route file: analytics.ts
router.get('/', (req, res) => res.json({ message: 'analytics route placeholder' }))

export default router
