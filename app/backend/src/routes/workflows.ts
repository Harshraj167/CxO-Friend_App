import express from 'express'
const router = express.Router()

// Placeholder route file: workflows.ts
router.get('/', (req, res) => res.json({ message: 'workflows route placeholder' }))

export default router
