import express from 'express'
const router = express.Router()

// Placeholder route file: notifications.ts
router.get('/', (req, res) => res.json({ message: 'notifications route placeholder' }))

export default router
