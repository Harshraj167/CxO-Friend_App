import express from 'express'
const router = express.Router()

// Placeholder route file: users.ts
router.get('/', (req, res) => res.json({ message: 'users route placeholder' }))

export default router
